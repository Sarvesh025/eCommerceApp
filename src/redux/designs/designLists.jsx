import { createSlice } from "@reduxjs/toolkit"

const initialState = {
    value: '',
  }

  export const designLists = createSlice({
    name:'designs',
    initialState,
    // reducers: {

    // }
  })

  export default designLists.reducer